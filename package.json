{
  "acode-plugin": "plugin.json",
  "type": "module",
  "private": true,
  "devDependencies": {
    "archiver": "^8.0.0",
    "esbuild": "^0.28.1"
  },
  "scripts": {
    "dev": "node build/esbuild.config.js --serve",
    "build": "node build/esbuild.config.js"
  }
}
